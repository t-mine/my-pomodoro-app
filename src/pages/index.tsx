import React from 'react';
import Header from '../components/Header';
import Timer from '../components/Timer';

const Home: React.FC = () => {
  return (
    <div>
      <main>
        <Timer />
      </main>
    </div>
  );
};

export default Home;
